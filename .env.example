# Copy this file to `.env` (`cp .env.example .env`) and adjust the values if needed.
# These variables are shared between the Docker dev container and local `uv` runs.
DATA_PATH=/home/workspace/data
OUTPUT_PATH=/home/workspace/out
LOGS_PATH=/home/workspace/logs
CONFIGS_PATH=/home/workspace/configs
